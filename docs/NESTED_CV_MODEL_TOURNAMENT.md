# Nested CV Model Tournament

_Required by section 58 of the final-validation prompt. Generated by `muleguard.cli.nested_report` - do not hand-edit; edit the generator._

<sub>Written 2026-08-13 17:43 UTC from the artifacts named in each section.</sub>

The flat tournament in `docs/MODEL_TOURNAMENT_REPORT.md` chose the shipped model. This document is the check on that choice: the same families, the same folds, but with selection and tuning moved inside the fold where they cannot see the rows they are scored on.

## 1. The protocol as run

Run written 2026-08-13T14:06:25.482811+00:00.

| element | as run |
|---|---|
| outer | stratified 5-fold x 3 repeats (identical to the flat tournament folds) |
| inner | stratified 4-fold within outer-train |
| selection | LightGBM gain ranking fitted on each inner-train partition only; frequencies pooled within the outer fold |
| tuning | bounded Optuna TPE, objective = inner-CV average precision |
| feature sizes offered | [30, 60, 120] |
| outer validation use | predicted once, never fed back |

Every outer-validation row is predicted exactly once per repeat, by a model whose features and hyperparameters were chosen without it. That is the whole difference from the flat tournament, and it is the reason this estimate is the primary one.

## 2. Leaderboard under the primary protocol

| family | nested PR-AUC | sd across repeats | ROC-AUC | features | seconds |
|---|---:|---:|---:|---:|---:|
| `catboost` | **0.80653** | 0.00845 | 0.9631 | 120 | 42133.5 |
| `histgb` | **0.76735** | 0.02949 | 0.9407 | 120 | 7710.0 |
| `xgboost` | **0.75393** | 0.00740 | 0.9437 | 120 | 39608.4 |
| `lightgbm` | **0.70046** | 0.02362 | 0.9295 | 120 | 1177.2 |
| `extratrees` | **0.54839** | 0.06962 | 0.9242 | 120 | 781.4 |
| `logistic_l1l2` | **0.16386** | 0.00308 | 0.8823 | 120 | 1576.4 |
| `dummy_prevalence` | **0.00910** | 0.00022 | 0.5098 | 120 | 0.3 |

`dummy_prevalence` is scored and never promotable: it exists so that a broken metric is visible as a dummy near the top rather than as a plausible number nobody questions.

## 3. What the flat protocol got wrong, and where

The flat tournament used the same outer folds, so the two columns are comparable. Flat is per configuration, nested is per family - each family's best admissible flat configuration is shown.

| family | nested (primary) | best flat | nested - flat | |
|---|---:|---:|---:|---|
| `catboost` | 0.80653 | 0.69630 (`catboost_top_60`) | +0.11023 | nested higher |
| `histgb` | 0.76735 | not contested | - | - |
| `xgboost` | 0.75393 | 0.76904 (`xgboost_top_120`) | -0.01511 | flat higher |
| `lightgbm` | 0.70046 | 0.69918 (`lightgbm_top_60`) | +0.00129 | nested higher |
| `extratrees` | 0.54839 | not contested | - | - |
| `logistic_l1l2` | 0.16386 | not contested | - | - |
| `dummy_prevalence` | 0.00910 | not contested | - | - |

**The flat protocol is not uniformly optimistic, and that is the finding.** Optimism appears where the flat run's single outside-the-folds choice happened to suit the family (`xgboost`), and the reverse appears where it did not. A family whose flat configuration was poor looks weak in the flat tournament for a reason that has nothing to do with how it generalises - which is exactly what a protocol that fixes the configuration once cannot see, and what the nested protocol was run to expose.

## 4. Does the shipped champion survive?

**Verdict: `CHAMPION_CHALLENGED`** - the nested protocol promotes catboost, not the shipped xgboost.

Shipped: `xgboost_top_120` (family `xgboost`, bundle 2.0.0). Nested promotes: `catboost`.

The gap is 0.0526 PR-AUC. Two families measured on 64 positives have wide, overlapping marginal intervals, so the question is not whether those overlap - it is whether the *difference* does, on rows both families scored.

| paired on identical rows | catboost - xgboost |
|---|---|
| mean difference | **0.05279** |
| 95 % interval | [0.02374, 0.08602] |
| excludes zero | yes |
| repeats favouring the challenger | 3/3 |
| bootstrap resamples favouring it | 1.0 of 1.0 (2000 replicates) |

These intervals are on the difference and are not comparable to the single-family intervals in `metric_battery.json`.

### What acting on this would cost

- re-fit the calibrator and the conformal layer on the new scores
- re-freeze the policy thresholds against the new score distribution
- re-open the locked test, which is single-touch by construction
- re-run the release suite, because every QA number names the model

This file records the finding. Swapping the champion is a decision with a locked-test cost attached and is not taken by a report.

So the finding is published and the bundle is not touched. A reader who wants the challenger shipped has the number, the interval and the bill in front of them.

## 5. The tuning question, closed

An open note asked whether the in-fold Optuna tuning was net-harmful, after an untuned arm beat a tuned one by 0.02333 on the same folds. Paired over all 15 outer folds it is not:

- gain from not tuning: **0.00205** (sd of the paired difference 0.02594)
- sign test 10/15 folds, p = 0.30176
- Wilcoxon p = 0.5614 · paired t p = 0.7636

The larger pooled gap is a pooling effect: the tuned arm loses 0.03304 PR-AUC when its folds are pooled against 0.01176 for the untuned one, because a configuration that changes per fold produces scores that do not share a scale. Full reasoning in `docs/TUNING_OVERFIT_HYPOTHESIS.md`.

## 6. The frozen thresholds under the primary protocol

The policy thresholds are frozen against `xgboost_top_120`'s flat score distribution. Applying those same numbers, unchanged, to the nested predictions of the same family answers whether the freeze describes the model or only the protocol that produced it.

| tier | frozen cut | alerts, flat → nested | precision | recall |
|---|---:|---:|---:|---:|
| `CRITICAL_REVIEW` | 0.9338487190474695 | 25 → 20 | 1.00000 → 1.00000 | 0.39062 → 0.31250 |
| `URGENT_REVIEW` | 0.09773832436704985 | 100 → 96 | 0.53000 → 0.53125 | 0.82812 → 0.79688 |
| `STANDARD_REVIEW` | 0.013182620557866929 | 334 → 382 | 0.17365 → 0.14136 | 0.90625 → 0.84375 |

Brier moves 0.00313 → 0.00328 and the skill against the base rate 0.64186 → 0.62495.

The largest move is `CRITICAL_REVIEW`, -5 alerts (-20.0%), and the tiers keep their order by volume. The cuts therefore describe the model rather than the protocol that produced it, and **no threshold is re-frozen and no calibrator is re-fitted on this evidence**. That is a decision with a reason attached rather than an omission.

Two honest qualifications. The tiers do not hold their **recall** as well as their volume - the deep tier gives up several points, which is what applying an unchanged cut to a slightly weaker score distribution does, and it is the cost of not re-freezing rather than an argument that nothing changed. And this covers the shipped family only: the challenger in section 4 scores on its own scale and would need its own freeze, which is part of the bill listed there.

## 7. What this run does not decide

- **Nothing was retrained here.** Every number is read from a stored prediction or a stored leaderboard.
- **The locked test was not opened.** It is single-touch and was spent on the shipped bundle; no nested result is checked against it.
- **No threshold moved.** The policy thresholds are frozen against the shipped model's score distribution and a different model would need its own freeze.
- **The nested run does not rank feature-set sizes.** Each fold chose its own from {30, 60, 120}; the leaderboard reports the mode, which is a description of what happened, not a recommendation.

## 8. Reproduce

```
.venv/Scripts/python.exe -m muleguard.cli.nested_cv --repeats 3 --inner 4
.venv/Scripts/python.exe -m muleguard.cli.nested_promotion
.venv/Scripts/python.exe -m muleguard.cli.tuning_overfit
.venv/Scripts/python.exe -m muleguard.cli.nested_report
```

The first command is the expensive one - it is a full re-run of the protocol, not a re-read - and the other three are seconds.

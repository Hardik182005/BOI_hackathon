# Final Validation Report

_Generated 2026-08-13T17:57:04+00:00 by `muleguard.cli.final_report`. Do not edit by hand — run `make final-report`, or `bash scripts/final_validation.sh`, which regenerates this along with the evidence it reads._

## 0. The answer

**Verdict: `FAIL`** — the §65 answer, reproduced from `artifacts/testing/final_verdict.json`, which is regenerated by `make final-verdict` and is the only place the verdict is decided.

- Champion: **`xgboost_top_120`**, promoted 2026-08-13T02:53:19.184309+00:00.
- Release blockers: 16/21 resolved, 5 open.
- Pass criteria: 14/20 met, 6 open.

## 1. What was validated

A mule-account detection system for BOI × IIT Hyderabad Hackathon 2026 PS2: 9,082 accounts, 81 positives (0.892 % prevalence), one workbook, no external data. The question this report answers is not "is the model good" but "is the reported number the number a bank would actually get" — for the deployed `xgboost_top_120` and for nothing else.

Class imbalance at this level breaks the usual reflexes: accuracy is 99.1 % for a model that predicts "legitimate" every time, so it is not used as a headline anywhere. Average precision (PR-AUC) and recall at a fixed analyst review budget are, because they are the two quantities a fraud desk actually lives with.

## 2. Which protocol decides

Two cross-validation protocols are run, and every number in this repository is labelled with the one that produced it.

**Flat repeated CV (3×5).** Feature selection and hyperparameter tuning happen once, over the whole development split; the folds then score the result. This is the protocol that promoted the shipped champion, and it is optimistic by construction — the selection saw every row it was later scored on. It is reported as such, never as the headline estimate.

**Nested repeated CV (primary).** Selection and tuning happen inside each outer fold, against inner splits only, and each outer-validation row is predicted exactly once. This is the estimate the project treats as its honest one.

```text
outer      : stratified 5-fold x 3 repeats (identical to the flat tournament folds)
inner      : stratified 4-fold within outer-train
selection  : LightGBM gain ranking fitted on each inner-train partition only; frequencies pooled within the outer fold
tuning     : bounded Optuna TPE, objective = inner-CV average precision
outer rows : predicted once, never fed back
```

**Nested leaderboard** (`artifacts/metrics/nested_cv.json`):

| family | PR-AUC mean | ± std | ROC-AUC | repeats |
|---|---|---|---|---|
| `catboost` | 0.80653 | 0.00845 | 0.96311 | 3 |
| `histgb` | 0.76735 | 0.02949 | 0.94071 | 3 |
| `xgboost` | 0.75393 | 0.00740 | 0.94366 | 3 |
| `lightgbm` | 0.70046 | 0.02362 | 0.92945 | 3 |
| `extratrees` | 0.54839 | 0.06962 | 0.92422 | 3 |
| `logistic_l1l2` | 0.16386 | 0.00308 | 0.88235 | 3 |
| `dummy_prevalence` | 0.00910 | 0.00022 | 0.50975 | 3 |


**The primary protocol promotes a different model from the one shipped.** The nested protocol promotes catboost, not the shipped xgboost. The gap on the leaderboard is 0.05260 PR-AUC. Paired on identical rows it is 0.05279, 95% CI [0.02374, 0.08602], excluding zero, with 3/3 repeats favouring the challenger - which is the test that matters, because two marginal intervals on 64 positives overlap whatever the truth is.

This is recorded rather than acted on. Swapping the champion is not a documentation change:

- re-fit the calibrator and the conformal layer on the new scores
- re-freeze the policy thresholds against the new score distribution
- re-open the locked test, which is single-touch by construction
- re-run the release suite, because every QA number names the model

The locked test is single-touch by construction, so acting on this spends evidence that cannot be regenerated. `make nested-promotion` exits non-zero while this stands, and `artifacts/metrics/nested_promotion_decision.json` carries the full leaderboard.

## 3. The leakage firewall, and what it cost

The firewall excludes columns that would not exist at the moment a real decision is made — the target itself, post-resolution status flags, and investigation-derived fields. Exclusion is from *every* model, selector, calibrator, explanation and export, not just from the final fit.

| column | name | availability class | why excluded |
|---|---|---|---|
| `F3924` | — | — | target variable (F3924) |
| `F3912` | — | — | target leak: measured |corr|=0.97, single-feature CV PR-AUC=0.94, balanced label reconstruction=0.99 |
| `F2230` | — | — | snapshot-month label artifact: ALL 9,001 negatives are the 2025-10 snapshot, ALL 81 positives are Sep/Nov/Dec snapshots - the month deterministically reconstructs the target (balanced reconstruction 1.0). Also invalidates any out-of-time split on this column. |
| `__UNNAMED__0` | — | — | row-index / identifier column |


This is also the reason the generation-1 champion was retired. `catboost_tuned_top60` scored a locked-test PR-AUC of 0.8242 on a feature pool that contained quarantined columns; that number is withdrawn, not improved upon, and the current figures are lower because they are measured without those columns. `docs/HISTORICAL_METRIC_RECONCILIATION.md` reconciles the two generations line by line, and `docs/LOCKED_TEST_RULING.md` records what the locked test may and may not still be used for.

## 4. What this report does not claim

- **No claim of perfect or near-perfect detection** — the champion misses positives at every review budget, and section F of `FINAL_VERDICT_A_L.md` gives the counts.
- **No claim of zero false positives** — a 100 %-precision row exists in the retired generation-1 artifacts and is not a property of the deployed model — `docs/HISTORICAL_METRIC_RECONCILIATION.md` reconciles the two.
- **No claim of a guarantee about any individual account** — the system outputs review tiers and evidence for an analyst, never a determination of guilt, and no output is a legal or factual finding.
- **No claim of superiority over other competitors or published systems** — no common evaluation has been run, so no comparative claim is available.
- **No claim of generalisation to a different bank, period, or product mix** — every estimate comes from one workbook of 9,082 accounts, and `docs/ADVERSARIAL_VALIDATION_REPORT.md` is the only evidence about shift.

The §61 honesty rules are enforced in code, not left to discipline, in three places: `assert_language_safe` rejects the forbidden verdict vocabulary in every payload before it leaves the process, release-gate check `no_forbidden_verdict_vocabulary` scans shipped source for the same words, and `test_the_docs_do_not_overclaim` scans this file and every other document for absolute claims. A hit fails the release suite, and a refusal that names what it refuses - the lines above - passes, which is the only exemption.

## 5. Evidence inventory (§57)

Every artifact §57 requires, and whether it is on disk. `make reconcile-artifacts` rebuilds the derived ones from their real sources, so a spec-named file cannot hold stale content while its source moves on.

28 required artifacts: **19** derived, **1** missing, **8** present.

Nothing is missing.

<details><summary>The 19 regenerated artifacts and their real sources</summary>

| spec name | derived from |
|---|---|
| `artifacts/testing/environment.json` | `artifacts/environment_snapshot.json` |
| `artifacts/testing/data_integrity.json` | `artifacts/testing/data_integrity_results.json` |
| `artifacts/testing/description_integrity.json` | `artifacts/features/feature_dictionary.json` |
| `artifacts/testing/leakage_results.json` | `artifacts/testing/leakage_test_results.json` |
| `artifacts/testing/nested_cv_results.json` | `artifacts/metrics/nested_cv.json` |
| `artifacts/testing/positive_removal_results.json` | `artifacts/metrics/nested_positive_removal.json`, `artifacts/metrics/stability_stress_v2.json` |
| `artifacts/testing/adversarial_validation.json` | `artifacts/metrics/nested_shift_shield.json` |
| `artifacts/testing/calibration_results.json` | `artifacts/metrics/lens_stack_oof_v2.json` |
| `artifacts/testing/threshold_results.json` | `artifacts/metrics/lens_stack_oof_v2.json` |
| `artifacts/testing/backend_results.json` | `artifacts/testing/backend_test_results.json` |
| `artifacts/testing/ui_metric_consistency.json` | `artifacts/testing/api_frontend_consistency.json` |
| `artifacts/metrics/final_nested_cv.json` | `artifacts/metrics/nested_cv.json` |
| `artifacts/metrics/final_budget_metrics.csv` | `artifacts/metrics/capacity_curve.json` |
| `artifacts/metrics/final_calibration.json` | `artifacts/metrics/lens_stack_oof_v2.json` |
| `artifacts/metrics/alert_context_ablation.csv` | `artifacts/metrics/alert_context_ablation_v2.json` |
| `artifacts/predictions/all_outer_oof_predictions.parquet` | `artifacts/predictions/nested_oof.parquet` |
| `artifacts/predictions/final_model_oof_predictions.parquet` | `artifacts/predictions/oof_v2.parquet` |
| `artifacts/features/final_selected_features.json` | `artifacts/features/selected_features_v2.json` |
| `artifacts/features/final_selection_frequency.csv` | `artifacts/features/selection_frequency_v2.csv` |


</details>

## 6. Experiment ledger (§60)

`artifacts/experiments/experiment_ledger.csv` records **121 experiments** (ACCEPTED 19, CHALLENGER_ONLY 1, DIAGNOSTIC 25, NOT_AN_EXPERIMENT 3, NOT_RUN 2, PENDING_RUN 7, PROVISIONAL 2, REFERENCE_ONLY 4, REJECTED 6, REJECTED_LEAKAGE 5, SUPERSEDED 47). The ledger is rebuilt from the artifacts themselves and exits non-zero if any file under `artifacts/metrics/` is not claimed by an entry, which is how "no forgotten experiments" is enforced rather than remembered. Negative and abandoned results stay in it on purpose: a ledger that only lists what worked is a highlight reel.

## 7. What is still open

**Release blockers still open**

| blocker | status | why |
|---|---|---|
| train-validation overlap | `STALE` | all checks passed, but artifacts/testing/leakage_results.json was recorded before xgboost_top_120 wa |
| preprocessing fitted on validation | `STALE` | all checks passed, but artifacts/testing/leakage_results.json was recorded before xgboost_top_120 wa |
| feature selection fitted on validation | `STALE` | all checks passed, but artifacts/testing/leakage_results.json was recorded before xgboost_top_120 wa |
| test labels used for tuning | `STALE` | all checks passed, but artifacts/testing/leakage_results.json was recorded before xgboost_top_120 wa |
| model artifact cannot reproduce saved predictions | `STALE` | all checks passed, but artifacts/testing/leakage_results.json was recorded before xgboost_top_120 wa |


**Pass criteria not yet met**

| criterion | status | what fills it |
|---|---|---|
| DataSet.xlsx fingerprinted | `NOT_MET` | workbook SHA-256 recorded and re-verified, but recorded before xgboost_top_120 was promoted |
| target verified | `NOT_MET` | all checks passed, but artifacts/testing/data_integrity.json was recorded before xgboost_top_120 was |
| semantic registry built | `NOT_MET` | feature dictionary built from Description.xlsx, but recorded before xgboost_top_120 was promoted |
| best candidate stability tested | `NOT_MET` | seed, positive-removal and rank stability measured, but recorded before xgboost_top_120 was promoted |
| distribution shift reported | `NOT_MET` | PENDING - produced by: python -m muleguard.cli.nested_ses --stages shift |
| one-command run works | `NOT_MET` | run.sh brought the stack up, but recorded before xgboost_top_120 was promoted |


A `STALE` blocker is not a failed check. It means the check passed against evidence written **before** the current champion was promoted, so it describes a different model and has to be re-run rather than re-read. `bash scripts/final_validation.sh` re-runs the whole battery and refreshes every one of them in a single pass.

## 8. Top risks

1. Evidence is incomplete: 6 of 20 pass criteria are not met yet (DataSet.xlsx fingerprinted, target verified, semantic registry built, best candidate stability tested, ...). Nothing here can be read as a final PASS until those runs land.
2. 5 release blockers are cleared only by evidence recorded before xgboost_top_120 was promoted. The QA suites must be re-run against the current bundle before the verdict means anything.
3. 64 positives at 0.88% prevalence. One mule is worth 1.6 recall points, so every interval in this report is wide and every fold-level difference is fragile.
4. Under positive removal the worst round fell to 0.6413 PR-AUC from a 0.7493 reference: the model's ranking depends on which mules it was shown.
5. The headline metrics come from the flat protocol, which is not the primary one. Under nested CV the shipped family scores 0.75393 against the flat 0.76904 (-0.01511). On the same rows `catboost` beats it by 0.05279 PR-AUC with a 95% interval excluding zero, and the champion was left in place rather than swapped. Read the headline as what the shipped artefact does, not as the best estimate available.

## 9. The other required reports (§58)

| report | what it answers |
|---|---|
| [`DESCRIPTION_VALIDATION_REPORT.md`](DESCRIPTION_VALIDATION_REPORT.md) | Description.xlsx parsed and reconciled against the data |
| [`FEATURE_AVAILABILITY_FIREWALL.md`](FEATURE_AVAILABILITY_FIREWALL.md) | which columns are excluded, and on what evidence |
| [`FINAL_LEAKAGE_FORENSICS.md`](FINAL_LEAKAGE_FORENSICS.md) | the leakage investigation in full |
| [`HISTORICAL_METRIC_RECONCILIATION.md`](HISTORICAL_METRIC_RECONCILIATION.md) | why older numbers in this repo differ from current ones |
| [`NESTED_CV_MODEL_TOURNAMENT.md`](NESTED_CV_MODEL_TOURNAMENT.md) | the primary-protocol tournament |
| [`HIDDEN_VALIDATION_READINESS.md`](HIDDEN_VALIDATION_READINESS.md) | what happens when the organisers hand over unseen data |
| [`POSITIVE_REMOVAL_STABILITY.md`](POSITIVE_REMOVAL_STABILITY.md) | how much the result depends on individual positives |
| [`ADVERSARIAL_VALIDATION_REPORT.md`](ADVERSARIAL_VALIDATION_REPORT.md) | dev-vs-test separability and shift-prone features |
| [`CALIBRATION_AND_THRESHOLDS.md`](CALIBRATION_AND_THRESHOLDS.md) | probability quality and the frozen policy thresholds |
| [`FALSE_POSITIVE_VALIDATION.md`](FALSE_POSITIVE_VALIDATION.md) | the cost of being wrong on a legitimate account |
| [`VALIDATION_LAB_TEST_REPORT.md`](VALIDATION_LAB_TEST_REPORT.md) | the upload flows a judge will exercise |
| [`SEALED_VALIDATION_PROTOCOL.md`](SEALED_VALIDATION_PROTOCOL.md) | how a labelled upload is scored without leaking into the model |
| [`UI_METRIC_CONSISTENCY.md`](UI_METRIC_CONSISTENCY.md) | the dashboard shows the artifacts' numbers, not its own |
| [`OFFLINE_RUNTIME_TEST.md`](OFFLINE_RUNTIME_TEST.md) | the system with the network removed |
| [`SECURITY_TEST_REPORT.md`](SECURITY_TEST_REPORT.md) | input handling, PII, and the LLM boundary |
| [`FINAL_MODEL_CARD.md`](FINAL_MODEL_CARD.md) | the model card for the deployed champion |
| [`FINAL_VERDICT_A_L.md`](FINAL_VERDICT_A_L.md) | the §65 answer in its required A-L form |


## 10. Reproducing this

```bash
# the judge path - verifies shipped artifacts, never retrains
bash scripts/final_validation.sh

# rebuild the expensive evidence from the raw workbooks (hours)
bash scripts/final_validation.sh --full-retrain

# the individual questions this report summarises
make nested-promotion   # does the primary protocol keep the champion?
make experiment-ledger  # is any experiment missing from the record?
make final-verdict      # the A-L answer, and the release decision
```

# No-MCP / No-Browser-Agent Compliance

Scan artifact: `artifacts/testing/no_mcp_scan.txt` (regenerated by
`scripts/test_security.sh`).

## Findings

| Check | Result |
|---|---|
| MCP configuration/servers in repo | **None** — no `.mcp.json`, no MCP references in application code |
| Claude-in-Chrome / browser-agent integrations | **None** |
| Playwright/Puppeteer/Selenium dependencies | **None installed** — frontend E2E uses Vitest + Testing Library (jsdom), invoked from the terminal (`npm test`) |
| External AI-agent services | **None** — no `anthropic`/`openai` SDKs in `requirements.lock` or `package.json` |
| LLM required to start/score | **No** — scoring path (`muleguard/models/scoring.py`) contains no LLM call; Ollama is an optional localhost narrator with a deterministic fallback and is probed, never required (`/health/ready` reports `"ollama_required": false`) |
| Internet required at runtime | **No** — model bundle, dataset parquet and all artifacts are local; the API binds 127.0.0.1 |

## How the requirement is met structurally

- Training, scoring, calibration, thresholding and policy decisions are pure
  Python over local artifacts (assertable: `grep -r "ollama\|llm" src/muleguard/models/`
  returns nothing but comments).
- The only network client in the core app is the *optional* `httpx` call to
  `http://localhost:11434` (Ollama) inside the narrator, wrapped in a circuit
  breaker and deterministic fallback; unit + integration tests run with Ollama
  absent.
- Frontend tests run headlessly in jsdom via `npm test` — no browser
  automation, no extensions.

**Verdict: PASS — the product runs entirely from the terminal with no MCP, no
Claude-in-Chrome, no browser agents, and no external AI services.**
